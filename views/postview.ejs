<%- include("partials/header.ejs") %>

<div class="container mt-5" style="width: 60%;">
    <% if (blog) { %>
        <h1 class="mb-4"><%= blog.title %></h1>
        <p><%= blog.content %></p>
    <% } else { %>
        <p class="alert alert-danger">Blog not found.</p>
    <% } %>
</div>

<%- include("partials/footer.ejs") %>